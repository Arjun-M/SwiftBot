[pytest]
asyncio_mode = auto
testpaths = tests
# Collect only ``*Tests`` classes so the public ``TestClient`` class in the
# testing harness is never mistaken for a test suite.
python_classes = *Tests
